# ============================================================
# Nutri-RAG Backend — Python Dependencies
# ============================================================
# Pinned to minor versions for reproducible builds.
# Run `pip install -r requirements.txt` inside a virtual environment.
# ============================================================

# --- Web framework ---
fastapi==0.115.0
starlette==0.38.6
uvicorn[standard]==0.30.6

# --- Data validation ---
pydantic>=2.7,<3.0

# --- Environment ---
python-dotenv>=1.0,<2.0

# --- HTTP client ---
requests>=2.32,<3.0

# --- Database ---
supabase>=2.4,<3.0

# --- LangChain / LangGraph ecosystem ---
langchain>=0.3,<0.4
langchain-groq>=0.2,<0.3
langchain-community>=0.3,<0.4
langgraph>=0.2,<0.3

# --- Observability ---
prometheus-fastapi-instrumentator>=7.0,<8.0
prometheus-client>=0.20,<1.0

# --- PDF ingestion (optional — only needed for ingest.py) ---
PyMuPDF>=1.24,<2.0
